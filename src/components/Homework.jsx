import React, {useState, useEffect} from 'react'

/*
1. Base on example code create counter which will store every new value in array inside state. 
2. After every 10 click/changes display message (e.g. with use of console log) for this case use 'useEffect' hook 
3. Keep tack of every 5 changes and display it on screen inside <div/>  ( after 5 you should display 1, after 10 it should be 2 etc. )
4. You should finish above tasks in less than 15 minutes. 
*/

const Homework = () => {

    return (
        <div>

        </div>
    )

}

export default Homework;
